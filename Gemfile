source "https://rubygems.org"

# Use github-pages to mirror GitHub Pages environment (pins compatible jekyll/plugins)
gem "github-pages", group: :jekyll_plugins

# Optional: if you want to run a specific jekyll locally, uncomment and adjust:
# gem "jekyll", "~> 4.2"
